{% extends 'base.html.twig' %}

{% block title %}SupportOfCourse{% endblock %}

{% block body %}
    <h1>SupportOfCourse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ support_of_course.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ support_of_course.title }}</td>
            </tr>
            <tr>
                <th>Fichier_url</th>
                <td>{{ support_of_course.fichierUrl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_teacher_support_of_course_index') }}">back to list</a>

    <a href="{{ path('app_teacher_support_of_course_edit', {'id': support_of_course.id}) }}">edit</a>

    {{ include('teacher_support_of_course/_delete_form.html.twig') }}
{% endblock %}
