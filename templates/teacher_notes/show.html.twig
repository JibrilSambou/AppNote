{% extends 'base.html.twig' %}

{% block title %}Notes{% endblock %}

{% block body %}
    <h1>Notes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ note.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ note.note }}</td>
            </tr>
            <tr>
                <th>Subject</th>
                <td>{{ note.Subject }}</td>
            </tr>
            <tr>
                <th>DateAdded</th>
                <td>{{ note.dateAdded ? note.dateAdded|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_teacher_notes_index') }}">back to list</a>

    <a href="{{ path('app_teacher_notes_edit', {'id': note.id}) }}">edit</a>

    {{ include('teacher_notes/_delete_form.html.twig') }}
{% endblock %}
